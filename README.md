# todo-web-socket installation

 clone the repository.
 cd todo-web-socket
 cd client
 run yarn/npm install
 run yarn/npm build
 back to root folder and run yarn/npm install
 run yarn dev

## open localhost://4000 in couple of browsers and enjoy.
